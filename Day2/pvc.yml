 apiVersion: v1
 kind: PersistentVolumeClaim
 metadata:
    name: myclaim
    namespace: "jegan-stateful-app"
 spec:
    storageClassName: manual 
    accessModes:
    - ReadWriteOnce
    resources:
      requests:
        storage: 1Gi
